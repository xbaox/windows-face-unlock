# ЭТАП 7 — HANDOFF (ротация окна архитектора)

> Читать ПЕРВЫМ при старте нового окна архитектора.
> Приложения: `face-unlock-MASTER-TZ.md`, `audit-notes.md` (реестр §J–M + «Этап 7-i»).
> Составлено Claude Code по итогам блоков 7a и 7a-bis, ветка `stage7-packaging`.

---

## §1 — СТАТУС

### Ветка

`stage7-packaging` (НЕ master), базируется на `master 0cd65b1`. **16 коммитов.**

### 7a — A–I, принято архитектором

| блок | хеш | что |
|---|---|---|
| A | `1e79fc7` | EOL: репо-wide `* text=auto`, 11 файлов в LF, исключение `dll.cpp`/`guid.h` отменено |
| B | `a9b11f7` | git-хаускипинг: 5 инертных записей + дубль `stage-2-RESUME.md` сняты, `/build/ /dist/ /installer_output/` добавлены, 7 слитых веток удалены |
| C | `07c35ab` | сняты три инертных кноба (`model_name`, `detector_backend`, `anti_spoofing`); полей стало **45** |
| D | `74ae28f` | `config.example.toml` пересобран (45/45 == дефолтам) + `tools/config_example_selftest.py`; `setup.ps1` больше не сеет боевой конфиг |
| E | `d0e8505` | сборка CP в `build-cp` через `-S/-B`, `rmtree` снят, провал CP валит сборку, шаг-дубль из CI удалён |
| F | `2f01c56` | `register.ps1` переписан: elevation, пробинг двух раскладок, верификация по реестру, exit 1/2/3 |
| G | `c685c17` | 4 регистратора → `tools/tasks.psd1` + `tools/register_tasks.ps1`; из `installer.iss` убраны `schtasks`×4, хардкод имён, `startuptray`, дубль-ярлык |
| H | `d43786f` | пурж старого ML-стека из упаковки, `download_weights.py` удалён, спека собирает InsightFace/ORT/CUDA, `installer/requirements-build.txt` |
| I | `db9edf2` | `INSTALL.md` переписан; исправлены два **инвертированных** утверждения (авто-логон тайла, `CUDA_VISIBLE_DEVICES`) |

### Стоп-фикс — принято архитектором

| хеш | что |
|---|---|
| `11d5aba` | `register_tasks.ps1`: устранён шейдоуинг `$action`→`$Action` (`[ValidateSet]` прилипает к имени регистронезависимо), введён two-phase порядок (A чистая / B commit), добавлен `-DryRun`. Попутно `-DryRun` на первом же прогоне поймал второй дефект: `return @(...)` разворачивается → `.Count` падает под `StrictMode` при одном элементе; все вызовы обёрнуты в `@()` по месту. |

### Живой смоук

**Холодный смоук пройден живьём:** 3 задачи с `PT0S`, 6 свежих процессов (3 пары), `verify` здоров под `paranoid`.

**Горячая перерегистрация поверх живой системы НЕ проводилась** — сознательно отложена в 7b как управляемый эксперимент. Причина: сразу после B3-kill живых процессов с persistent-камерой случился инцидент (двум инстансам подряд клинило камеру/инициализацию). Гипотеза — жёсткий kill при живой капче провоцирует клин следующего `open`/`read`. Подробности — §2.

### O — handoff

| хеш | что |
|---|---|
| `640deb5` | `docs(stage7): handoff — architect window rotation` — этот файл |

### 7a-bis — J–N, сданы и приняты архитектором 2026-07-28

7a-bis J–N (`62c9c70..1ef92d6`) сданы и приняты архитектором 2026-07-28.

| блок | хеш | что |
|---|---|---|
| J | `62c9c70` | оба README переписаны под реальность; снята дезинформация про `NULL DACL` / `_build_sa_everyone` и старый ML-стек |
| K | `dbd779f` | комментарии `__main__.py` и `service.py` приведены к факту (доказано comment-only: AST идентичен); трём селфтестам добавлен `sys.path` bootstrap → **18/18** script-style |
| L | `de1e6f9` | restart-скрипты свёрнуты в `-Action Restart` регистратора (B3+B4, без B1/B2); `restart_service.ps1` удалён, `clean_restart.ps1` — обёртка |
| M | `389eaf6` | `requirements.lock` перекодирован в UTF-8/LF; пины доказательно не менялись (sha256 нормализованного текста совпал) |
| N | `1ef92d6` | `-DryRun` для `register.ps1`; доказано по AST: единственный мутирующий вызов ниже гейта. НЕ запускался |

### План оставшихся блоков

- **7b камера ✅ ЗАКРЫТ 2026-07-28** — 8 коммитов, все на origin. Подробности — §3.
- **7c поведение — КОД СДАН, closeout pending** — `9449479` (тихий watchdog), `27fbede` (однотредовый overlapped ping + классы провала + `_load_config`), `4fd37ff` (три защиты `auto_lock`: дефекты камеры → `camera-error`, `presence_fullscreen_strikes`, C-гейт `session_locked`). Впереди живые смоуки; их доказательства + закрытие блока — отдельным closeout-коммитом. Разбор решений — `audit-notes.md` «Этап 7 / 7c».
  ⚠️ Пункт «тюнить `watchdog_ping_timeout_s` живым конфигом» **снят как опровергнутый реконом**: вотчдог живой конфиг не читал вовсе (`Config()` вместо `Config.load()`), а сам блип был мгновенным отказом коннекта на занятом одноинстансовом сервере — бюджет 2.0 с не тратился, и поднятие таймаута не лечило ничего. Решено ретраем коннекта внутри того же бюджета плюс классификацией причины.
- **7d ядро упаковки 🔴** — валидация конфига на 45 полей, структурные логи, подпись DLL + инструкция, апдейт-механизм, uninstaller без следов, watchdog-Installed фикс: третья frozen-точка входа + mode-aware `_MATCH_PS` + флип `InstalledExe` + живой тест, провижининг `buffalo_l`, первый `-DryRun` `register.ps1` — за Bao.
- **7e SYSTEM-custody** — решить судьбу (+TPM-seal той же судьбой).
- **7f финал** — security-чеклист MASTER §7 попунктно, доки-свод, судьба `service.py`-рефакторинга, чистая установка/эмуляция, финальный ребут-прогон, мерж `--no-ff`, handoff stage-8; ПОСЛЕДНИМ пунктом — напомнить Bao выключить System Protection на C:.

### Открытое после 7a + 7a-bis

Список «за периметром A–I» отработан блоками J–N. Осталось три пункта:

1. **`TF_NUM_INTRAOP_THREADS` / `TF_NUM_INTEROP_THREADS` в `face_service/__main__.py`** — мёртвые переменные: TensorFlow ушёл на Этапе 2. Комментарий рядом теперь честно называет их вестигиальными, но сами строки живы: их удаление — правка кода, а не комментария, и под doc-only санкцию блока K не попадало.
2. **`tools/watchdog.py:9`** — докстринг ссылается на `tools/clean_restart.ps1` как на носитель критерия матчинга процессов. После L критерий живёт в `tools/register_tasks.ps1`, а `clean_restart.ps1` — тонкая обёртка. Правка докстринга в рантайм-файле, вне периметра L.
3. **Горячая перерегистрация поверх живой системы** — припаркована в 7b как управляемый эксперимент (см. §2 и план блоков выше).

---

## §2 — ИНЦИДЕНТЫ КАМЕРЫ 2026-07-27 (материал recon 7b)

Контекст: смоук перерегистрации 15:33:01 жёстко убил 6 живых процессов с persistent-капчей; далее три инстанса сервиса подряд.

**Экспонат A — инстанс 15:33:01:** camera warmup ok (15:33:08), первый же verify (15:33:27) завис НАВЕЧНО — ни ответа, ни MSMF-таймаута (на этом железе read-timeout не срабатывает, известное), лог замолк; вотчдог убил; клиент получил 109 pipe-ended. НОВЫЙ вариант §K: не «чёрные кадры с ответом distance 1.0 / ~7200мс», а вечный read без ответа.

**Экспонат B — инстанс 15:34:03:** 97 секунд не дошёл даже до «InsightFace ready», убит вотчдогом. Новая сигнатура; кандидат — уборка CUDA/камерного контекста после жёсткого kill предшественника; причина НЕ установлена.

**Экспонат C — инстанс 15:35:41:** здоров; вотчдог вылечил систему за 3 цикла (реабилитирован); 5 полных paranoid+жест входов в 15:36 и вход в 17:17. Архитектор затем снёс здоровую систему по устаревшей картине — урок: полные улики ДО сноса. «Чёрные окна» вотчдога = долг «тихий watchdog» (7c), теперь с боевой иллюстрацией.

**Минорное:** окно занятости ~15:36:55→15:37:47 (два провальных пинга + пропуск presence-тика), рассосалось само, причина не установлена.

**Frame Server чист:** штатная «Камера» работала при мёртвых наших процессах — клин НЕ system-wide. Утренний кейс (distance 1.0 / ~7200мс БЕЗ визарда) — родственный класс. Гипотеза 7b: жёсткий kill процесса с живой persistent-капчей провоцирует клин open/read у следующего владельца; проверять управляемо, откаты по цене: `Start-ScheduledTask` → полный сброс → `Restart-Service FrameServer` → ребут.

### Разрешение (блок 7b, 2026-07-28)

**§1 KNOWN_ISSUES закрыт** (`ea6f9ef`). Корень — визард в отдельном процессе (`7f2a6bc`): смерть процесса заставляет ОС отдать камерный хэндл независимо от зависшего `read()`. Плюс три страховки: самолечение отравленного кэша (`7cda7f2`), лиза 120 с с ре-армом 45 с (`05bdd4b`), жёсткий кап одиночного open (`d15bc15`). **Доказано смоуком 7b-3:** баг-условие ВОСПРОИЗВЕЛОСЬ (`enroll camera thread did not exit within 3s` в 18:58:40), последствий ноль — следующий `verify` дал `distance 0.2137` / `real:true` вместо прежних `1.0` и чёрных кадров до рестарта.

**§2 KNOWN_ISSUES (клин Frame Server) — НЕ воспроизведён** управляемым экспериментом 7b-4 (2026-07-28). Прогон 1 (до фикса, 19:41): hard kill живых процессов с persistent-капчей — клин НЕ наступил, LED восстановился, `verify` здоров. Прогон 2 (после фикса, 20:06): graceful-цепочка отработала, сервис вышел сам за 1.3 с, kill достался только presence/watchdog, watchdog-пауза записана. **Вердикт:** одиночные чистые прогоны гипотезу не опровергают (27.07 мог быть гонкой), но `fd84f88` ставит graceful pipe-shutdown перед hard kill в обеих мутирующих ветках `register_tasks.ps1` — штатные `Register`/`Restart` живую капчу больше не убивают. Прежний запрет на `clean_restart.ps1` снят.

**Горячая перерегистрация поверх живой системы** (пункт 3 «Открытого после 7a + 7a-bis») — проведена, это и есть прогон 1 выше. Долг закрыт.

---

## §3 — Статус ветки stage7-packaging на закрытии блока 7b

### 7b — камера, ЗАКРЫТ 2026-07-28

**8 коммитов, все на `origin/stage7-packaging`, дерево чисто, `master 0cd65b1` не тронут.**

| хеш | что |
|---|---|
| `2d6b5e6` | `verify` отдаёт `verdict` пятым полем (форма `unlock` не менялась) |
| `7cda7f2` | самолечение отравленного persistent-кэша: zero-frame / чёрный burst роняет кэш, verify переснимает один раз |
| `d15bc15` | `BoundedOpener` — жёсткий кап ожидания ОДНОЙ попытки open + реклейм брошенной капчи |
| `7f2a6bc` | энролл-визард вынесен в отдельный процесс; трей хранит `Popen` и снимает его на Quit |
| `05bdd4b` | лиза 300→120 с с ре-армом каждые 45 с из камерного цикла |
| `ea6f9ef` | `KNOWN_ISSUES §1` закрыт, заведён `§2` (клин Frame Server) |
| `0089b52` | каноничное имя логгера визарда + стартовая info-строка |
| `fd84f88` | graceful pipe-shutdown перед hard kill в `Register`/`Restart` |

### Скоуп-пункты 1–7 прежнего §4 — все закрыты

1. **Диагностика idle-залипания** — причина установлена: кэш `self._cam` реюзается по единственному условию `is not None`, health-check отсутствует, read-фейл везде `continue`, реопена нет. Лечится `7cda7f2` пост-фактум (probe-read сознательно НЕ добавлен: на этом железе он сам может зависнуть и повесить последовательный пайп-сервер).
2. **Вынос визарда + координация владения** — `7f2a6bc` (процесс + `_launch_enroll`/`_terminate_enroll` + хранимый `Popen`) и `05bdd4b` (лиза с ре-армом). Мёртвый визард отдаёт камеру за ≤120 с.
3. **Реальный open-deadline + cap** — `d15bc15`: `BoundedOpener` (тред + `join(cap_s)`), кооперативный `deadline` в `open_fast`, новое поле `camera_open_attempt_cap_s`; переобещающие «never hangs» переписаны честно во всех трёх файлах.
4. **Lease-продление** — `05bdd4b`, ре-арм привязан к камерному циклу намеренно: заклинивший цикл перестаёт продлевать, лиза истекает именно тогда, когда должна.
5. **`KNOWN_ISSUES §1`** — `ea6f9ef`, закрыт с доказательством смоуком.
6. **Горячая перерегистрация** — проведена (эксперимент 7b-4), клин не воспроизвёлся; страховка `fd84f88`. Детали — §2.
7. **Вердикт `NEEDS_GESTURE` в `verify`** — `2d6b5e6`, поле `verdict` добавлено пятым; форма `unlock` и контракт CP не тронуты.

### Живое

Смоук 7b-3: баг-условие §1 воспроизвелось и последствий не дало (см. §2). Эксперимент 7b-4: два прогона, оба чистые. Топология здорова — 6 процессов, `verify` здоров под `paranoid`.

### Известные пробелы (не 7b)

Watchdog в Installed-раскладке объявлен, но skip (нет frozen-точки + `_MATCH_PS` не матчит exe) — фикс в 7d; спека сборкой не проверялась; `register.ps1` живьём НЕ запускался (первый прогон — 7d); dev-vs-frozen ветка спавна визарда — `TODO(packaging)` в `tray.py::_launch_enroll`, блок упаковки.

### Открытое после 7a + 7a-bis — остаток

Пункт 3 (горячая перерегистрация) закрыт в 7b, пункт 2 (докстринг `tools/watchdog.py:9`) — в 7c коммитом `9449479`. Жив один:

1. **`TF_NUM_INTRAOP_THREADS` / `TF_NUM_INTEROP_THREADS` в `face_service/__main__.py`** — мёртвые переменные, TensorFlow ушёл на Этапе 2; удаление — правка кода, под doc-only санкцию не попадало. → 7d/7f.

---

## §4 — ПРОМПТ ОКНА 7c

```
Прокачиваю опенсорс caochitam/windows-face-unlock (face-логин с настоящим Credential Provider) до уровня Windows Hello на ASUS TUF FA507XI — Win11, Python 3.12, RTX 4070, RGB-вебка без ИК, вход по PIN. Форк xbaox/windows-face-unlock, локально C:\dev\windows-face-unlock.
Приложены: face-unlock-MASTER-TZ.md, audit-notes.md (секция «Этап 7» — вердикт эксперимента 7b-4; §K камера — первоисточник; §M TODO Этапа 3 — тихий watchdog), stage-7-TZ.md (§3 — статус ветки после 7b, §1 — план блоков этапа, §2 — камерные инциденты и их разрешение), KNOWN_ISSUES.md (§1 закрыт, §2 со страховкой).
Этап 7 идёт блоками на ветке stage7-packaging. Блоки 7a, 7a-bis и 7b закрыты. Это окно — блок 7c: ПОВЕДЕНИЕ. Скоуп: (1) тихий watchdog — tools/watchdog.py спавнит powershell и schtasks через subprocess.run БЕЗ creationflags, под задачей на pythonw это мигающие чёрные консольные окна (audit-notes §M пункт «(b) тихий режим watchdog», боевая иллюстрация — §2 stage-7-TZ); (2) ping-блипы — единичные «ping failed (1/3)» каждые ~6.5 мин, watchdog_ping_timeout_s=2.0 стоит впритык, до 2/3 не доходит, рестартов ноль: решить ТЮНИТЬ живым конфигом или ПРИНЯТЬ и задокументировать; сам дефолт заморожен, смена — отдельной санкцией; (3) auto_lock-тюнинг — поле родилось True и не менялось, у Bao в живом конфиге False (dev-преференс, не репо-дефолт); риск ложных срабатываний: fullscreen-игры, idle-контенция за камеру (audit-notes §K); (4) A3-field — предикат «сессия залочена» (presence_monitor/remote_session.py::session_locked): на ЭТОЙ машине работает только ветка access-denied, ветка «имя десктопа = Winlogon» у medium-IL не отрабатывает; на чужой машине деградирует в статус-кво — не ломается, но и не работает; (5) i18n-решение — 10 языков на EN-фолбэке, полные только en/ru; 7b завёл три новых поля конфига (camera_open_attempt_cap_s, camera_black_luma, camera_reopen_cooldown_s) с ключами тоже только EN+RU: принять как политику или добить, низкий приоритет.
Процесс: кодит Claude Code (CC, НОВОЕ окно). Ты — архитектор: промпты для CC + ревью. Я релею живьём. Мне по-русски, на «ты», кратко, код НЕ давай — только промпты для CC. Recon → моё ОК → код. Живые команды всегда с нуля (полный cd C:\dev\windows-face-unlock, .\ перед .venv\Scripts\python.exe).
Рантайм: CC живой рантайм не запускает — сервис, scheduled tasks, камера, pipe-команды к живому сервису, ребуты запрещены; файлы, py_compile, git, grep — можно. Две стоячие санкции: (а) register_tasks.ps1/clean_restart.ps1 ТОЛЬКО с -DryRun и ТОЛЬКО после AST-доказательства, что -DryRun выходит до фазы B; (б) селфтесты из tools/ CC гоняет САМ, но только после собственного аудита по коду — не открывает камеру и не коннектится к пайпу живого сервиса, не пишет в прод-файлы (конфиг/галерея/аудит-лог), не берёт продовый именованный мутекс; для camera_busy_selftest.py и config_example_selftest.py аудит уже сделан и действителен, любой другой — сначала аудит и доклад. Если классификатор прав заблокирует запуск — стоп и доклад, не обходить другим инструментом.
Состояние на входе: HEAD = docs(stage7)-хендофф этого батча поверх fd84f88, ветка синхронна с origin, дерево чисто. Здоровый прод = 6 процессов (Service/Presence/Watchdog ×2, PT0S). Живой конфиг: paranoid, threshold 0.32, adaptive_gallery=true, persistent_camera=true (LED горит при живом сервисе — НОРМА), pipe_*=true, auto_lock=false, verify_required=2, ru; Config = 48 полей. Здоровая сигнатура verify под paranoid = match:false + real:true + verdict NEEDS_GESTURE при distance ~0.21–0.27 (гуляет по свету) — это НЕ «не узнал»; поле verdict в ответе verify появилось в 7b (2d6b5e6). Галерея 30×512. KNOWN_ISSUES §1 ЗАКРЫТ (визард в отдельном процессе), §2 (клин Frame Server) не воспроизведён и прикрыт страховкой. Рестарт стека теперь штатный: .\tools\clean_restart.ps1 (graceful-first) либо вручную pipe shutdown → Start-ScheduledTask.
Заморозки: числа (0.32, STRONG_MARGIN 0.10, liveness/lockout/adaptive/low-light/camera/watchdog, enroll_qc), verify_frame/_prep_cuda_dlls байт-в-байт, серверный периметр Этапа 4 (SYSTEM-гейт unlock, DACL, анти-сквоттинг), C++ CP заморожен (санкция 7-i исчерпана); константы правятся ТОЛЬКО через новые поля конфига по санкции поштучно. EOL LF (CR байтами), git diff перед коммитом, ревью-гейт держать (диффы показывать и ждать ОК на каждый), селфтесты на .venv 3.12, контрольные grep'ы только надёжным инструментом (bash-grep врёт — прецедент 7a). Файлы stage-*-TZ.md трекаются обычным образом — правила в .gitignore на них НЕТ, коммитить как всё остальное (проверено 2026-07-28: git check-ignore пуст, файл в индексе).
System Protection на C: ВКЛЮЧЕНА — не трогать (выключение = явный последний пункт приёмки всего Этапа 7).
Тайминг: первый вход после ЛЮБОГО ребута = PIN (by design).
Начни: статус-чек (git log --oneline -3 на stage7-packaging — жду HEAD с docs(stage7)-хендоффом поверх fd84f88, git status, процессы — жду 6) + промпт CC на read-only recon поведенческого периметра (tools/watchdog.py — спавн подпроцессов и ping-цикл; presence_monitor/remote_session.py — предикат лока; presence_monitor/monitor.py — auto_lock и страйки; face_service/i18n.py — покрытие ключей по языкам) → покажи → дизайн-ревью → жди моих подтверждений.
```
