# Этап 8 — Security-чеклист MASTER §7 и сверх: финальная сверка

Дата: 2026-09-25. Основание: 8a `security-checklist.md` (HEAD `c05a981`), фиксы 8b / 8b-2 (сборка 0.1.1
`1d5b8c0`), живая приёмка 8c-1…8c-8b. Номера F-NN / D-NN — из 8a `findings.md` / `debts.md`.
Детали воспроизведения атак сознательно опущены: в файле только последствия и фиксы.

Вердикты: **PASS** · **ПРИНЯТО**.

## MASTER §7 по пунктам

| # | Пункт | 8a | Итог | Чем закрыто и улика |
|---|---|---|---|---|
| 1 | GUID свой, апстримовый не остался нигде | PASS | **PASS** | Код и реестр (8a); комментарий `guid.h` (D-34) — 8b |
| 2a | Пайп: явный DACL SELF+SYSTEM без Everyone + Medium label | PASS | **PASS** | Живой SDDL (8a); `pipe_hardening_selftest` |
| 2b | FIRST_PIPE_INSTANCE + loud-fail | PASS | **PASS** | Ключ `pipe_first_instance` добавлен в храповик (8b, F-20) |
| 2c | Клиент проверяет server-SID | PARTIAL | **PASS** | Все Python-клиенты (трей, монитор, мастер, вотчдог, `pipe_client`) ходят через `pipe_io` с проверкой сервера и дедлайном (8b, F-20); в 8c все они работали на установленной копии |
| 2d | `unlock` гейтится на SYSTEM, дефолт True | PASS | **PASS** | Живьём 8c-3: LogonUI прошёл SID-гейт при SQOS identification (F-26); строк «rejected» за 8c — 0 |
| 2e | Оба posture-ключа под храповиком | PASS / PARTIAL по смыслу | **PASS** | Посадка больше не держится на открытом каталоге: custody с fail-closed (8b, F-01), живьём 8c-1 |
| 3 | ~~Подпись / nonce~~ | ПРИНЯТО (СНЯТО) | **ПРИНЯТО (СНЯТО)** | «Этап 4 — threat-model» |
| 4a | Пароль: DPAPI user-scope + per-install энтропия | PASS | **PASS** | `credentials.py`, `credentials_selftest` |
| 4b | Недоступен другим учёткам | FAIL (F-01, P0) | **PASS** | Права Users на каталог и `[Dirs]` убраны из инсталлятора; сервис лечит ACL каталога при старте и отказывает (`insecure-data-dir`), если не смог (8b-A). Живьём 8c-1: снято 124 чужих ACE, APP_DIR protected SELF / SYSTEM / BA, `verify_data_dir` — 0 проблем; идемпотентно — 8c-5, 8c-8, 8c-8b |
| 4c | `pipe_entropy.bin` рождается запертым | FAIL | **PASS** | F-23 (8b-A); живьём у обоих секретов — protected, ровно SELF (учётка Bao) и SYSTEM (8c-1, 8c-8, 8c-8b) |
| 4d | Same-user disclosure | ПРИНЯТО | **ПРИНЯТО** | Этап 7e |
| 4e | TPM-seal | ПРИНЯТО-НЕ-ОТЛОЖЕНО | **ПРИНЯТО-НЕ-ОТЛОЖЕНО** | Этап 7e |
| 5 | Гигиена: две категории, единый классификатор, рекурсивный обход | PASS / FAIL цели | **PASS** | Биометрия больше не читаема другими учётками (F-01); `debug_frames\` вычищается при старте (F-12 / D-16), живьём 8c-1 — 40 файлов |
| 6a | Liveness бьёт фото / видео / экран | PARTIAL | **PASS** | Флаг экрана действует и в раунде жеста (8b, F-11); живьём 8c-8 — попытка с фото отбита (NOT_LIVE) |
| 6b | Rate-limit + lockout активны | PASS | **PASS** | Сбой камеры / движка не даёт страйка (F-18); `reset_lockout` удалён (F-21); живьём — страйк и сброс следующим грантом (8c-3 → 8c-4, 8c-8) |
| 6c | Аудит-лог пишется | PASS | **PASS** | Записи во всех окнах 8c; `granted` пишется только после доставки (F-19, живьём 8c-3) |
| 7a | CP: строгий таймаут | PARTIAL (F-03) | **PASS** | Бюджет ожидания пайпа читается один раз и не бывает 0 (8b, F-03); живьём 8c-8b — сервиса нет, плитка за 12 с отвечает «service unavailable» |
| 7b | Гарантированный фолбэк на PIN / пароль | PARTIAL (F-04) | **PASS** | Проверка пароля при сохранении (8b-B); CP читает результат входа и глушит сканы (8b-I); живьём 8c-4; PIN при закрытой камере (8c-3) и до логона (8c-8b) |
| 7c | Off на RDP | PASS | **PASS** | Код CP |
| 7d | Строго аддитивный, не фильтр | PASS | **PASS** | В Filters нашего CLSID нет (8c-1) |
| 8 | Uninstaller без следов, проверяемо | PARTIAL | **PASS** | F-34 `RunOnceId`, F-39, F-07, F-02 — 8b; живой деинсталл 0.1.1 — ПРИНЯТО не прогонять (D-30, улики 7g) |

## Сверх §7

| Проверка | 8a | Итог | Чем закрыто и улика |
|---|---|---|---|
| Запись в PF и в DLL | PASS | **PASS** | Нестандартный каталог установки отключён (`DisableDirPage`, 8b, F-09) |
| ACL реестра CP / CLSID | PASS | **PASS** | — |
| Зависимости DLL | PASS | **PASS** | Только системные |
| Подпись DLL | FAIL | **PASS** | Подпись обязательна в сборке (8b, F-13); живьём 8c-1, 8c-8, 8c-8b — подписант `13F9BB62…649C` |
| Dev-регистрация CP | PARTIAL | **PASS** | F-10 (8b-H); живой CP указывает в PF (8c-1, 8c-8b) |
| Апдейтер | PARTIAL (спящий) | **ПРИНЯТО** | Notify-only до подписанных релизов (8b-F, P-01; KNOWN_ISSUES §8); живьём — `releases/latest` 404, установки нет |
| Вход пайпа как недоверенный | PASS с хвостами | **PASS / ПРИНЯТО** | NaN / inf (F-15, F-16) и кривой JSON (F-42) — 8b; нет таймаута чтения (F-14, same-user DoS) — ПРИНЯТО, KNOWN_ISSUES §7 |
| Единственная точка выдачи пароля | PASS | **PASS** | `_release_credentials` — за SYSTEM-гейтом, lockout и токеном фазы 2 |
| Токен / пароль в логах | PASS | **PASS** | 0 / 0 |
| Цепочка сборки | PARTIAL | **PASS** | Frozen-гейт в сборке и пины CI (8b-H, F-08 / F-38); пасс 7 «lazy» и живой frozen-самотест custody (8b-2) |

## Итог

§7 **пройден**: все пункты — PASS или ПРИНЯТО. Чекбоксы MASTER §7 заполнены, заголовок — «сверка Этапа 8».
Новые долги 8c — D-40 и D-41 (P3, ПРИНЯТО; D-41 — KNOWN_ISSUES §9).
