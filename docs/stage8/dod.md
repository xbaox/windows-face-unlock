# Этап 8 — Definition of Done (MASTER §6): финальная сверка

Дата: 2026-09-25. Основание: 8a `dod.md` (аудит, HEAD `c05a981`), фиксы 8b / 8b-2 (`276f560..97b44d0`,
сборка 0.1.1 `1d5b8c0`), живая приёмка 8c-1…8c-8b на установленной 0.1.1. Улики — `C:\dev\d-series\8a\`,
`8b\`, `8b2\`, `8c\`. Номера F-NN / D-NN — из 8a `findings.md` / `debts.md`; D-40 и D-41 заведены в 8c.

Вердикты: **PASS** · **ПРИНЯТО**.

| # | Пункт DoD | 8a | Итог | Чем закрыто и улика |
|---|---|---|---|---|
| 1 | Вход лицом с реального локскрина, стабильно, доли секунды | PASS | **PASS** | Фаза 1 — 188 мс (8c-3), 203 мс (8c-4, 8c-8), 218 мс (8c-8b); живое лицо Bao на локскрине за 8c не отклонялось ни разу. Повторный прогон после ребута (D-31): 8c-8 — Restart, 3 входа лицом; 8c-8b — холодный старт |
| 2 | Отшивает плоское фото, видео с телефона, статичный экран | PARTIAL | **PASS** | Раунд жеста считает кадр с флагом экрана чужим (8b, F-11); селфтесты liveness / verdict / gesture_round зелёные. Живьём 8c-8: попытка Bao с фото — NOT_LIVE (экран 4/5 и 2/5), вход не выдан. Видео с телефона в 8c живьём не прогонялось — принято по Этапу 2 и селфтестам |
| 3 | Пайп закрыт OS-границей; храповик; секрет DPAPI недоступен другим юзерам | FAIL | **PASS** | Каталог данных под custody с fail-closed (8b: F-01, F-23, F-02, F-12); `pipe_first_instance` под храповиком, server-SID проверяют все клиенты (F-20). Живьём 8c-1: снято 124 чужих ACE, секреты заперты SELF+SYSTEM, `verify_data_dir` — 0 проблем; на рестартах 8c-5 и ребутах 8c-8 / 8c-8b — `aces_removed=0 problems=0`. SYSTEM-гейт при SQOS identification — живьём 8c-3 (F-26) |
| 4 | Same-user disclosure | ПРИНЯТО | **ПРИНЯТО** | Этап 7e; пере-аудит 8a — основания в силе |
| 5 | TPM-seal | ПРИНЯТО | **ПРИНЯТО** | Этап 7e (ПРИНЯТО-НЕ-ОТЛОЖЕНО); пере-аудит 8a |
| 6a | Свой GUID | PASS | **PASS** | Код и реестр (8a); устаревший комментарий `guid.h` исправлен (8b, D-34) |
| 6b | Чистый uninstall | PASS с хвостами | **PASS** | Хвосты закрыты в 8b: `RunOnceId` (F-34), phase A без админа (F-39), исходный юзер по SID (F-07 — подтверждён живой установкой 8c-1), удаления без хождения по reparse (F-02). Живой деинсталл 0.1.1 не прогонялся — ПРИНЯТО (D-30, улики 7g) |
| 6c | Страховка от лок-аута работает | PARTIAL | **PASS** | F-04 живьём 8c-4: неверный сохранённый пароль → ровно 1 грант, сканы остановлены, текст на плитке, PIN пускает. PIN при закрытой камере — 8c-3. До логона, когда сервиса ещё нет, плитка за 12 с пишет «service unavailable», PIN пускает (8c-8b, F-03). Вотчдог поднимает зависший и упавший сервис (8c-5) |
| 7a | Быстрый режим субсекундный | PASS | **PASS** | 8c-6: фаза 1 без жеста — 203–266 мс при уверенном совпадении |
| 7b | Параноик заметно строже | PASS | **PASS** | Жест на каждом входе (8c-3, 8c-4, 8c-8, 8c-8b); в paranoid адаптации галереи нет |
| 7c | Оба режима переключаются | PARTIAL | **PASS** | 8c-7: fast через `reload_config`, тонкий запас 0.0871 < 0.10 → `needs-gesture` → blink → granted; возврат в paranoid, SHA конфига = эталон (D-06) |
| 8 | Полированный энролл и настройки; RU | PARTIAL | **PASS** | «Удалить энролл» останавливает распознавание (F-06), «Заменить / Добавить» (F-12) — 8b и селфтесты; дубли мастера и трея (F-31) — живьём 8c-7; коуч позы (D-17) — ПРИНЯТО по селфтесту, живьём — при естественном переэнролле; RU 229 / 229 (8a) |
| — | Упаковка: подпись DLL, минимум self-signed (MASTER §3) | FAIL | **PASS** | Подпись обязательна в сборке (8b, F-13); живьём 8c-1, 8c-8, 8c-8b — подписант `13F9BB62…649C` (статус UnknownError ожидаем: dev-корень не доверен) |

## Итог

DoD **выполнен**: все пункты — PASS или ПРИНЯТО. Остаток Этапа 8 — D-37 (System Protection off), отдельным
последним окном по санкции Bao. Принятые ограничения — KNOWN_ISSUES §6–§9.
