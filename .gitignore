.venv/
__pycache__/
*.pyc
.idea/

# Build output: PyInstaller work dir + dist, Inno Setup artefacts.
/build/
/dist/
/installer_output/

# C++ build trees (CMake output, not sources).
# build-cp/ holds the LIVE credential-provider DLL that LogonUI loads at the
# lock screen -- ignore it, but do NOT delete it.
build-cp/
build-tests/
